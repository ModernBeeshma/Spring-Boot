Spring boot is auto configurable framework.
STS (Spring Tool Suite) is used for this framework.
No need of external server.
Each project has its own internal sever which are configurable.
@RestController ,@Service ,@Controller ,@Repository are all used to make them to store in the spring container.
@Autowire is used to map the bean and classes which are stored in the spring container using various annotations.
